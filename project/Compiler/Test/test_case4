
let
    function isInRange (low : int, high : int, val : int) =
        (low <= val & val <= high)
in
    (
        println(isInRange(1, 3, 2));
        println(isInRange(4, 5, 6));
        println(isInRange(4, 5, 3))
    )
end