let
    function add (x : int, y : int) = x + y
    function sub (x : int, y : int) = x - y
    function pracFunc (x : int, y : int) = add(x, y) * sub(x, y)
in
    let
        var a := 3
        var b := 1
    in
        println (pracFunc (a, b))
    end
end