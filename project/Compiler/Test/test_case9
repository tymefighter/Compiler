let
    function fact (n : int) =
        let
            var x := 1
        in
            (
                if (n > 1) then x := n * fact(n - 1);
                x
            )
        end
in
    print(fact(10))
end