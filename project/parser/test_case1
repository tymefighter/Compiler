/*
    Author: Tymefighter
    Test Case: 1

    This is a test program which is being
    used to test if the indent program 
    (parser) for the tiger language works 
    correctly or not.
*/

let
    import "uart"
    import "math"
    import "arrlib"
    var u := 42
    type freqrec = {integ : i, floatno : flt}
    var eps := getValuEps()
    type itok = array of lltok
    function getAcquire (epsilon : ep, theta : th) : integ = ep + theta / 5
in

    for abc := 0 to 10 do
        if uart.timer = timeSet then
            (while (u.eps < 400) do 
                if 4 < 5 then 
                    if p.time[45] < v[u.eps].span then 
                        break 
                    else 
                        pr.getVal(400000))
        else
            uart.printValue.AllowInt(500)

end